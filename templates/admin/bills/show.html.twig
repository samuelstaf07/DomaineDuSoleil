{% extends 'base.html.twig' %}

{% block title %}Bills{% endblock %}

{% block body %}
    <h1>Bills</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bill.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ bill.content }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ bill.date ? bill.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Total_price</th>
                <td>{{ bill.totalPrice }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ bill.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bills_index') }}">back to list</a>

    <a href="{{ path('app_bills_edit', {'id': bill.id}) }}">edit</a>

    {{ include('admin/bills/_delete_form.html.twig') }}
{% endblock %}
