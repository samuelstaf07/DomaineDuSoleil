{% extends 'base.html.twig' %}

{% block title %}Nouvelle location{% endblock %}

{% block body %}
    <div class="contentMargin d-flex flex-column align-items-center justify-content-center mb-5 gap-3">
        <div class="d-flex w-75 justify-content-start">
            <a href="{{ path('app_rentals_index') }}" class="btn-yellow">Retour à la liste</a>
        </div>
        <div class="w-75 bg-lightGreen d-flex flex-column p-5 rounded-4">
            <h1>Ajouter une nouvelle location</h1>

            {{ include('admin/rentals/_form.html.twig') }}
        </div>

    </div>
{% endblock %}
