{% extends './base.html.twig' %}

{% block title %}RentalImages{% endblock %}

{% block body %}
    <h1>RentalImages</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rental_image.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rental_images_index') }}">back to list</a>

    <a href="{{ path('app_rental_images_edit', {'id': rental_image.id}) }}">edit</a>

    {{ include('admin/rental_images/_delete_form.html.twig') }}
{% endblock %}
