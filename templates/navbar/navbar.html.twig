{% block body %}
    <nav class="navbar navbar-expand-lg px-4 py-3 fixed-top" style="background: linear-gradient(to right, #006633, #95C11F);">
        <a class="navbar-brand align-items-center d-flex" href="{{ path('app_home') }}">
            <img src="{{ asset('img/DomaineduSoleilNoName.svg') }}" alt="Logo" class="me-2" height="50">
            <h2>Domaine Du Soleil</h2>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto flex gap-2">
                {% if app.user %}
                    <div class="dropdown">
                        <button class="d-flex gap-2 align-items-center btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <img src="{{ asset('uploads/user.svg') }}" alt="" class="rounded-circle" style="height: 30px; width: 30px;">
                            {{ app.user.firstname }} {{ app.user.lastname }}
                        </button>
                        <ul class="dropdown-menu">
                            {% if is_granted('ROLE_ADMIN') %}
                                <li>
                                    <a href="{{ path('app_admin') }}" class="dropdown-item text-center">
                                        Administration
                                    </a>
                                </li>
                                <li><hr class="dropdown-divider"></li>
                            {% endif %}
                            <li class="text-center">
                                <a href="{{ path('app_logout') }}" class="m-2 btn btn-primary w-75">Déconnexion</a>
                            </li>

                        </ul>
                    </div>
                {% else %}
                    <li class="nav-item">
                        <a class="btn btn-success" href="{{ path('app_login') }}">Connexion</a>
                    </li>
                    <li class="nav-item">
                        <a class="btn btn-secondary" href="{{ path('app_register') }}">Inscription</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </nav>
{% endblock %}